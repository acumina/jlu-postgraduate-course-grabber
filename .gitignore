# ============================================================
# .gitignore —— 这些东西**永远不要**提交到 GitHub
# ============================================================
# 最重要的一条：logs/ 里有你的抓包与日志 —— 学号、姓名、你已选的全部课程、
# 会话令牌都在里面。一旦 push 出去就是公开泄露（而且 git 历史很难清）。
logs/
*.har
*.jsonl

# 例外：test/fixtures 下的夹具是**假数据**，测试需要它（记得用 2099 开头的假学号）
!test/fixtures/
!test/fixtures/*.har

# 本地调试产物
.edge-debug/
.cache/
*.log

# 编辑器 / 系统
.vscode/
.idea/
Thumbs.db
desktop.ini
*.swp

# 提交信息的临时文件（git commit -F 用；别提交进去）
.git-commit-msg.txt
.*-commit-msg.txt

# 依赖（本项目零依赖，正常不会出现）
node_modules/

# ------------------------------------------------------------
# 个人数据（下面这些文件如果包含你的选课清单，也建议不提交）
# ------------------------------------------------------------
# 说明：kx-config-*.json 是"项目配置源"，可以提交（不含个人信息）；
# 但如果你在里面放了 targets（你的选课清单），提交前先清空 —— 
# 用 node tools/scan-secrets.mjs 检查一遍。
# 课程档案（archives/*.json）是学校的公开课表，可以提交（用户明确希望一起开源）。
